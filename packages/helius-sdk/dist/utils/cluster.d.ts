import { Cluster } from "@solana/web3.js";
/**
 * Retrieves the Helius RPC API URL for the specified cluster
 */
export declare function heliusClusterApiUrl(apiKey: string, cluster?: Cluster): string;
