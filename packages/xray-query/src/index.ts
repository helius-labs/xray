export const createQueryContext = ({
    setContext,
    browser,
} : {
    setContext: Function,
    browser: boolean
}) => {
    console.log({
        setContext,
        browser,
    });
};

interface Options {

}

export const createQuery = () => {
    
};
